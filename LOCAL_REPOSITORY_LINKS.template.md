# Local repository links (template)

Copy this file to `LOCAL_REPOSITORY_LINKS.md` in the repository root after clone and fill in paths to each checkout on your machine. `LOCAL_REPOSITORY_LINKS.md` is gitignored; only this template is tracked.

AI agents should read `LOCAL_REPOSITORY_LINKS.md` before cross-repo implementation work. If `LOCAL_REPOSITORY_LINKS.md` is missing or any value below is still `TBU`, stop and ask the developer to create or update `LOCAL_REPOSITORY_LINKS.md`.

Use absolute paths or paths relative to this workspace repository root.

## tateca

- backend: TBU
- infrastructure: TBU
- ios: TBU
- landing-page: TBU

## Notes

- Use `N/A` when a surface does not exist for that product.
- Prefer stable directory names (for example a repo root containing `build.gradle.kts`).
